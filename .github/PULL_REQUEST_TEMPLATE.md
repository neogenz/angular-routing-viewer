## What

<!-- What does this PR change, and why? -->

## How tested

<!-- Which Angular app / route shapes did you run it against? -->

## Checklist

- [ ] `bun tsc --noEmit` passes
- [ ] `bun run build` succeeds
- [ ] No `Bun.*` runtime APIs added to `src/` or `bin/` (stays Node-compatible)
- [ ] Docs / `CHANGELOG.md` updated if behavior changed
